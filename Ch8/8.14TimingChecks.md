# 8.14 Timing Checks

1. **$setup(data_event, ref_event, setup_time)**
2. **$hold(ref_event, data_event, hold_time)**
3. **$skew(ref_event, data_event, limit)**: displays a warning if skew is above limit
4. **$width(ref_event, limit)**: displays a warning if pulse width is shorter than limit
